\documentclass[aspectratio=169,10pt]{beamer}
\input{../theme}
\usepackage{amsmath,amssymb}
\definecolor{Best}{HTML}{157553}
\newcommand{\best}[1]{\textcolor{Best}{\textbf{#1}}}
\newcommand{\worst}[1]{\textcolor{Rust}{#1}}
\input{data/numbers}
\graphicspath{{figures/}}
\setbeamertemplate{footline}{\hspace{8mm}\textcolor{Muted}{\scriptsize SSBI · Task 2 · 40,000 NK cells}\hfill\textcolor{Muted}{\scriptsize\insertframenumber}\hspace{8mm}\vspace{3mm}}
\hypersetup{pdftitle={Task 2: Dimensionality reduction},pdfauthor={SSBI group project}}
\begin{document}

\begin{frame}{Which structures survive a two-dimensional view?}
  \begin{columns}[T,onlytextwidth]
  \begin{column}{.53\textwidth}
    \textcolor{Teal}{\large Data from Horowitz et al. (2013)}\par\medskip
    Mass cytometry (CyTOF): simultaneous protein-marker measurements on individual cells.\par\medskip
    \begin{tabular}{lr}\toprule
      Provided donors & 20\\
      Analysis markers & 37\\
      Available gated NK cells & 261,593\\
      Balanced analysis sample & 40,000\\\bottomrule
    \end{tabular}
  \end{column}
  \begin{column}{.42\textwidth}
    \textcolor{Teal}{\large Our task}\par\medskip
    Visualise the same cells with \textbf{PCA, t-SNE and UMAP}; show parameter effects and compare the maps quantitatively.\par\medskip
    \textbf{Project context:} CellCNN later tests whether cellular patterns predict donor CMV status. That performance comparison is not established by these maps.
  \end{column}
  \end{columns}
  \takeaway{CMV labels describe donors; they do not identify infected individual cells.}
  \source{Horowitz et al., Sci. Transl. Med. 2013; Arvaniti \& Claassen, Nat. Commun. 2017. Provided 20-donor subset; notebook 02\_dimensionality\_reduction\_clean.}
\end{frame}

\begin{frame}{Transform intensities, then equalise marker scales}
  \begin{columns}[c,onlytextwidth]
  \begin{column}{.58\textwidth}
    \includegraphics[width=\linewidth]{arcsinh.pdf}
  \end{column}
  \begin{column}{.38\textwidth}
    \[a_{ij}=\operatorname{asinh}(x_{ij}/5)\]
    Approximately linear near zero; compresses large magnitudes and accepts negative background values.\par\medskip
    \[z_{ij}=\frac{a_{ij}-\bar a_j}{s_j}\]
    Marker-wise z-scoring prevents large marker scales from dominating distances. All 37 markers pass the variance filter.
  \end{column}
  \end{columns}
  \takeaway{The geometry depends on preprocessing as well as on the projection method.}
  \source{Notebook: cofactor 5; variance threshold 0.05 on the arcsinh scale; pooled scaling across the balanced exploratory sample. Curve is a mathematical illustration.}
\end{frame}

\begin{frame}{Does the balanced sample preserve the selected phenotype?}
  \begin{columns}[c,onlytextwidth]
  \begin{column}{.49\textwidth}
    \centering\includegraphics[height=54mm]{subsample.png}
  \end{column}
  \begin{column}{.47\textwidth}
    Compare \textbf{2,000 cells per donor} with all available gated NK cells from that donor.\par\medskip
    NKG2C$^+$/CD57$^+$ is a marker phenotype associated with \textbf{memory-like (``adaptive'') NK cells} after CMV exposure. It is not itself a functional memory test.\par\medskip
    Points lie close to the identity line. No donor has fewer than 10 flagged cells in the balanced sample.
  \end{column}
  \end{columns}
  \source{Saved plot, cell 17 (counting from 1). Positivity: two-component GMM crossover per marker on all 261,593 cells. This check concerns one approximate phenotype, not every rare population.}
\end{frame}

\begin{frame}{29 PCs retain 91.50\%; the plotted two retain only 20.29\%}
  \begin{columns}[c,onlytextwidth]
  \begin{column}{.45\textwidth}
    \includegraphics[width=\linewidth]{pca_spectrum.pdf}
  \end{column}
  \begin{column}{.53\textwidth}
    \includegraphics[width=\linewidth]{pca_donor.pdf}
  \end{column}
  \end{columns}
  \medskip
  \textbf{Retention choice:} 29 PCs reach the 90\% threshold and become the input to t-SNE/UMAP.\par
  \textbf{Display choice:} PC1/PC2 show substantial donor overlap, but omit most retained variance.
  \source{40,000 cells, arcsinh + z-scoring. Spectrum verified from the same 37-marker covariance matrix; donor plot uses committed PCA coordinates. Notebook counterparts: cells 20--22.}
\end{frame}

\begin{frame}{t-SNE: changing perplexity changes the neighbourhood scale}
  \begin{columns}[c,onlytextwidth]
  \begin{column}{.52\textwidth}
    \centering\includegraphics[height=51mm]{tsne_selected.pdf}
  \end{column}
  \begin{column}{.44\textwidth}
    \textbf{5,000 fixed cells, 29 PCs}: reduce sweep time and memory.\par\smallskip
    Perplexity is an effective neighbourhood size, not an exact neighbour count.\par\smallskip
    \textbf{Selected: 60}, using the largest saved Trustworthiness:\par
    \smallskip
    \begin{tabular}{rr}50 & 0.925973\\60 & \best{0.926046}\\100 & 0.925800\end{tabular}\par\smallskip
    \small $T$: penalises false neighbours by their original ranks.\par
    $R$: fraction of original neighbours retained. The choice here uses $T$ alone.
  \end{column}
  \end{columns}
  \source{Selected panels from saved cell 29; colours indicate donor CMV status. The very small score gap is not evidence of a stable optimum. Invalid saved sweep kNN scores are omitted.}
\end{frame}

\begin{frame}{UMAP: neighbourhood scale versus point packing}
  \begin{columns}[c,onlytextwidth]
  \begin{column}{.50\textwidth}
    \centering\includegraphics[height=51mm]{umap_selected.pdf}
  \end{column}
  \begin{column}{.46\textwidth}
    \textbf{n\_neighbors}: size of the local neighbourhood used to construct the graph.\par\smallskip
    \textbf{min\_dist}: controls how tightly points can pack in the embedding.\par\medskip
    \small\input{data/umap_top}\par\medskip
    \normalsize Selected: \textbf{5 neighbours, min\_dist = 0.0}. Compact islands do not prove distinct cell types.
  \end{column}
  \end{columns}
  \source{Four corner settings from the 16-panel saved grid, cell 33; colours indicate CMV status. Top three rows ranked by saved Trustworthiness on the same 5,000 cells and 29 PCs.}
\end{frame}

\begin{frame}{Local preservation and global distances favour different maps}
  \centering\includegraphics[width=.90\textwidth,height=26mm,keepaspectratio]{embeddings_cmv.pdf}\par
  \smallskip\small\input{data/quality}\par\smallskip
  \begin{minipage}{.94\textwidth}\small
    $T,R$: local structure. $r_d$: Pearson correlation of pairwise distances. $S_{\rm CMV}$: separation by donor CMV label.\par\smallskip
    \textbf{t-SNE leads locally; PCA leads in distance correlation.}\quad All CMV silhouettes are close to zero.
  \end{minipage}
  \source{40,000 cells; reference: 29 PCs; k=15. Distance correlation: fixed 2,000-cell sample. T and silhouette are saved outputs; R is corrected. Green/red denote numerical best/worst within a column, not clinical validity.}
\end{frame}

\begin{frame}{Which maps are most similar? It depends on the measure}
  \begin{columns}[c,onlytextwidth]
  \begin{column}{.51\textwidth}
    \small\input{data/pairwise}\par\medskip
    \normalsize\textbf{Chosen task measure: Procrustes disparity $M^2$.}\par\smallskip
    Align two maps by translation, rotation/reflection and scaling; measure the remaining mismatch.
  \end{column}
  \begin{column}{.44\textwidth}
    \textbf{Local neighbours:} t-SNE and UMAP are the most similar pair.\par\medskip
    \textbf{Aligned global shape:} PCA and t-SNE are the most similar pair.\par\medskip
    These answers differ because the measures ask different questions.
  \end{column}
  \end{columns}
  \takeaway{Use maps to generate hypotheses; assess clusters and donor-level predictions in the later tasks.}
  \source{All three pairs evaluated on the same 40,000 cells in the same order. R(15) corrected from saved coordinates; SciPy Procrustes values independently reproduced. Neither metric identifies biologically correct cell types.}
\end{frame}

\appendix
\begin{frame}{Backup: how t-SNE turns neighbours into a map}
  \small
  \setlength{\abovedisplayskip}{2pt}\setlength{\belowdisplayskip}{2pt}
  \begin{align*}
    p_{j|i}&=\frac{\exp(-\lVert x_i-x_j\rVert^2/(2\sigma_i^2))}{\sum_{k\ne i}\exp(-\lVert x_i-x_k\rVert^2/(2\sigma_i^2))},\quad
    p_{ij}=\frac{p_{j|i}+p_{i|j}}{2n},\\
    q_{ij}&=\frac{(1+\lVert y_i-y_j\rVert^2)^{-1}}{\sum_{k\ne l}(1+\lVert y_k-y_l\rVert^2)^{-1}},\qquad
    C=\sum_{i\ne j}p_{ij}\log\frac{p_{ij}}{q_{ij}}.
  \end{align*}
  \small Bandwidth $\sigma_i$ matches the target perplexity $2^{H(P_i)}$. The heavy-tailed kernel helps alleviate crowding; optimisation minimises KL divergence.\par\smallskip
  \small\begin{tabular}{lp{.69\textwidth}}\toprule
    Varied & Perplexity: 5, 30, 50, 60, 70, 100.\\
    Fixed & 2 dimensions; PCA initialisation; seed 42; 750 iterations; Barnes--Hut method.\\
    Defaults & Euclidean metric, early exaggeration 12, automatic learning rate, angle 0.5.\\\bottomrule
  \end{tabular}\par\smallskip
  Other parameters were held fixed to limit the experiment; they can also change the map.
  \source{van der Maaten \& Hinton (2008); scikit-learn TSNE documentation. Fixed iteration count alone is not a convergence check. Defaults refer to the documented scikit-learn 1.9 environment.}
\end{frame}

\begin{frame}{Backup: two local metrics, two different penalties}
  Let $N_i^X(k)$ and $N_i^Y(k)$ be the $k$ neighbours of cell $i$, excluding itself.\par
  \[
    R(k)=\frac{1}{n}\sum_{i=1}^n\frac{|N_i^X(k)\cap N_i^Y(k)|}{k}
  \]
  \small Symmetric; exact neighbour identities matter. Ten shared neighbours out of fifteen contribute $10/15$. This is overlap/recall, not Jaccard ($10/20$ in that example).\par
  \[
    T(k)=1-\frac{2}{nk(2n-3k-1)}\sum_i\sum_{j\in N_i^Y(k)\setminus N_i^X(k)}(r_X(i,j)-k).
  \]
  \small Penalises new neighbours according to their rank in the reference. A point ranked 16th incurs a smaller penalty than one ranked 1,000th; direction matters.\par\medskip
  \textbf{Correction:} requesting neighbours with no explicit query already excludes self. Removing the first returned column retained 14 neighbours but divided by 15. The corrected identity score is 1.
  \source{Venna \& Kaski (2001); scikit-learn trustworthiness and NearestNeighbors documentation. Here k=15. Full-sample scores average cells; equal cells per donor give equal donor weight.}
\end{frame}

\begin{frame}{Backup: how UMAP constructs and embeds a weighted graph}
  \small
  \[w_{j|i}=\exp\!\left[-\frac{\max(0,d(x_i,x_j)-\rho_i)}{\sigma_i}\right],\qquad
    w_{ij}=w_{j|i}+w_{i|j}-w_{j|i}w_{i|j}.\]
  Offset $\rho_i$ and scale $\sigma_i$ adapt to local density; directed neighbourhoods are combined into symmetric weights.\par
  \[v_{ij}=(1+a\lVert y_i-y_j\rVert^{2b})^{-1},\qquad
    L\sim-\sum_{i<j}\{w_{ij}\log v_{ij}+(1-w_{ij})\log(1-v_{ij})\}.\]
  Sampled updates balance attraction and repulsion. The parameters $a,b$ depend on min\_dist and spread.\par\smallskip
  \begin{tabular}{lp{.67\textwidth}}\toprule
    Swept & n\_neighbors: 5, 15, 30, 50; min\_dist: 0, 0.1, 0.3, 0.5.\\
    Fixed/default & 2 dimensions, seed 42, Euclidean metric, spectral initialisation, spread 1.\\\bottomrule
  \end{tabular}\par\smallskip
  Larger neighbourhoods change the scale of the graph; larger min\_dist discourages tight packing. Neither guarantees accurate global distances.
  \source{McInnes, Healy \& Melville (2018/2020); official UMAP parameter and algorithm documentation. Equations describe the conceptual objective, not every optimisation approximation.}
\end{frame}

\begin{frame}{Backup: UMAP sweep, 5 and 15 neighbours}
  \centering\includegraphics[width=.96\textwidth,height=60mm,keepaspectratio]{umap_grid_1.pdf}
  \source{First two rows of the original 16-panel grid. Columns: min\_dist = 0, 0.1, 0.3, 0.5. Same 5,000 cells, 29 PCs and seed 42; each panel reports saved Trustworthiness.}
\end{frame}

\begin{frame}{Backup: UMAP sweep, 30 and 50 neighbours}
  \centering\includegraphics[width=.96\textwidth,height=60mm,keepaspectratio]{umap_grid_2.pdf}
  \source{Last two rows of the original 16-panel grid. Columns: min\_dist = 0, 0.1, 0.3, 0.5. Visual changes are parameter effects within one run, not a stability study across seeds.}
\end{frame}

\begin{frame}{Backup: marker expression on the UMAP map}
  \centering\includegraphics[width=.98\textwidth,height=56mm,keepaspectratio]{umap_markers.png}
  \source{Original saved output, cell 41: 40,000 balanced cells, not all 261,593 available NK cells. Arcsinh-transformed marker values; colour limits use the 1st/99th percentiles separately for each marker.}
\end{frame}

\begin{frame}{Backup: the same markers on the t-SNE map}
  \centering\includegraphics[width=.98\textwidth,height=56mm,keepaspectratio]{tsne_markers.png}
  \source{Original saved output, cell 41. Colour patterns are exploratory expression gradients. A bright region does not establish an infected-cell population or a validated adaptive-NK gate.}
\end{frame}

\begin{frame}{Backup: Shepard diagrams compare distances, not neighbours}
  \centering\includegraphics[width=.95\textwidth,height=29mm,keepaspectratio]{shepard.png}\par
  \begin{columns}[c,onlytextwidth]
    \begin{column}{.37\textwidth}\centering\includegraphics[height=25mm]{shepard_ideal.pdf}\end{column}
    \begin{column}{.59\textwidth}\small
      Each point is a pair of cells. $r_d=\operatorname{corr}(d_X,d_Y)$ uses all pairs among 2,000 sampled cells.\par\smallskip
      Exact distance equality gives the identity line. Uniform rescaling gives another straight line with $r_d=1$; therefore the diagonal is not required for perfect correlation.
    \end{column}
  \end{columns}
  \source{Top: saved notebook diagrams, 1,999,000 distances per representation. Bottom: schematic illustration, not experimental data. Pearson distance correlation is not the distance-correlation statistic of independence testing.}
\end{frame}

\begin{frame}{Backup: Procrustes aligns the maps before measuring mismatch}
  Centre and normalise each coordinate matrix to unit Frobenius norm:
  \[ A_c=\frac{A-\bar A}{\lVert A-\bar A\rVert_F},\qquad B_c=\frac{B-\bar B}{\lVert B-\bar B\rVert_F}.\]
  \[M^2=\min_{s,Q:\,Q^\top Q=I}\lVert A_c-sB_cQ\rVert_F^2.\]
  \smallskip
  \begin{itemize}
    \item Rows must correspond to exactly the same cells in the same order.
    \item Rotation/reflection, translation and uniform scaling do not count as biological differences.
    \item Lower disparity means a closer global fit after alignment; it does not establish local fidelity or class separation.
    \item Nonlinear deformations can change neighbour identities and global shape differently.
  \end{itemize}
  \source{SciPy scipy.spatial.procrustes. With its normalisation and optimal scaling, the scalar disparity is symmetric even though the returned transformed matrices have different normalisation conventions.}
\end{frame}

\begin{frame}{Backup: sources and verification scope}
  \small
  \textbf{Data and biology}\par
  Horowitz et al. (2013). \emph{Genetic and environmental determinants of human NK cell diversity revealed by mass cytometry.} Sci. Transl. Med. 5:208ra145.\par
  Arvaniti \& Claassen (2017). \emph{Sensitive detection of rare disease-associated cell subsets via representation learning.} Nat. Commun. 8:14825.\par\medskip
  \textbf{Methods}\par
  van der Maaten \& Hinton (2008), JMLR 9:2579--2605 (t-SNE).\par
  McInnes, Healy \& Melville (2018/2020), arXiv:1802.03426 (UMAP).\par
  Venna \& Kaski (2001), ICANN, pp. 485--491 (Trustworthiness).\par
  Official scikit-learn and SciPy documentation; linked references in the detailed report.\par\medskip
  \textbf{Provenance}\par
  Notebook source: main commit \texttt{252415a}. kNN corrected from committed coordinates; Procrustes and distance correlations checked. Embeddings, sweep Trustworthiness and CMV silhouettes were not retrained/recomputed. No new classification benchmark was run.
\end{frame}
\end{document}
